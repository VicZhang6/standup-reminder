{
  "name": "standup-reminder",
  "version": "1.5.1",
  "description": "站立提醒应用 - 每隔一段时间提醒你站起来活动",
  "license": "MIT",
  "author": "Vic Zhang",
  "scripts": {
    "dev": "node scripts/run-tauri.mjs dev",
    "dev:web": "vite --host 127.0.0.1 --port 1420",
    "build": "node scripts/run-tauri.mjs build",
    "build:web": "vite build",
    "build:win": "node scripts/run-tauri.mjs build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-opener": "^2.5.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "vite": "^8.0.1"
  }
}
